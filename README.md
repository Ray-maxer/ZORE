# ZORE

A minimal countdown timer script that lets you set a duration in minutes.

## Usage

```bash
python timer.py --minutes 1.5
```

The script counts down the requested time, updates the remaining time once per second, and prints a message when the timer finishes.
